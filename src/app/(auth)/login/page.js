import { LoginForm } from "@/features/auth/components/LoginForm";

export const metadata = {
  title: "Sign in | TalentProof Sales CRM",
};

export default function LoginPage() {
  return <LoginForm />;
}
